{
  "name": "nobl-air-dashboard",
  "version": "1.0.0",
  "description": "NOBL Air executive dashboard — reads Google Sheets, serves React frontend",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "build": "cd client && npm run build",
    "setup": "npm install && cd client && npm install && npm run build"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "compression": "^1.7.4",
    "connect-sqlite3": "^0.9.15",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "googleapis": "^140.0.0",
    "helmet": "^7.1.0",
    "node-cron": "^3.0.3",
    "node-sqlite3-wasm": "^0.8.56",
    "session-file-store": "^1.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
